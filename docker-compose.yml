services:
  web:
    build: .
    image: ghcr.io/pdpablo/spook-shack-v2:${SPOOK_SHACK_V2_IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      SPOOK_SHACK_DEMO_PASSWORD: ${SPOOK_SHACK_DEMO_PASSWORD:-SpookShack123!}
    volumes:
      - spook-shack-data:/app/data

volumes:
  spook-shack-data:
